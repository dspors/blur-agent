{
  "name": "blur-agent",
  "version": "0.3.0",
  "description": "Agent subsystem for blur-ai-runtime — durable AI-session identity + Turn record + live Reply API. Wraps pool.lease() to give every project-attached AI session a typed Agent record. sendMessage opens a Turn record (canonical 'what happened' log), mirrors reply chunks into it, collects side-effects via audit subscription. sendText returns a LiveReply for host-side ergonomics.",
  "license": "UNLICENSED",
  "private": true,
  "main": "dist/pack/index.js",
  "scripts": {
    "build": "tsc"
  },
  "blur": {
    "packs": [
      "dist/pack"
    ]
  },
  "dependencies": {
    "blur-ai-runtime": "file:../blur-ai-runtime"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18"
  }
}
